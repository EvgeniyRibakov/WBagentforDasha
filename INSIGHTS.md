# Инсайты и опыт работы над проектом

Этот документ содержит важные инсайты, опыт работы над различными подходами и информацию, которая поможет быстро включиться в работу над проектом.

## 🎯 Цель проекта

Автоматизация скачивания детализированных отчётов о продажах из Wildberries API для 6 кабинетов (COSMO, MMA, MAB, MAU, DREAMLAB, BEAUTYLAB). Отчёты должны соответствовать структуре файла `example_parse.xlsx` с колонками:
- Бренд, Предмет, Сезон, Коллекция, Наименование
- Артикул поставщика, Номенклатура, Баркод, Размер
- Контракт, Склад
- Заказано шт, Заказано себестоимость
- Выкупили шт, Выкупили руб
- Текущий остаток

## 📊 Структура проекта

```
WBagentforDasha/
├── src/
│   ├── wb_sales_parser.py      # Основной класс для работы с API
│   └── wb_report_downloader.py  # Скрипт для массового скачивания
├── data/                        # Отчёты по датам (автоматически)
├── example_parse.xlsx          # Шаблон желаемой структуры отчёта
└── .env                        # Токены для 6 кабинетов
```

## 🔄 Эволюция подходов

### Подход 1: Прямой API `/api/v1/supplier/sales` ❌
**Проблема:** Возвращает только базовые данные о продажах без детализации (бренд, предмет, склад и т.д.)

**Результат:** Отказались от этого подхода, так как не содержит нужных полей.

### Подход 2: API `reportDetailByPeriod` (v1, v2, v5) ⚠️
**Проблемы:**
- v1 и v2 возвращают 404 (эндпоинты не найдены или устарели)
- v5 возвращает пустые списки для ежедневных данных
- v5 работает только для еженедельных отчётов, которые формируются на следующей неделе

**Результат:** Не подходит для ежедневных отчётов в реальном времени.

### Подход 3: Новый Analytics API (`seller-analytics-api.wildberries.ru`) ⚠️
**Проблемы:**
- `DETAIL_HISTORY_REPORT` возвращает воронку продаж (dt, openCardCount, addToCartCount, ordersCount), а не детализацию продаж
- `STOCK_HISTORY_REPORT_CSV` требует дополнительные обязательные параметры (currentPeriod, stockType, skipDeletedNm, availabilityFilters, orderBy)
- Некоторые кабинеты получают 403 (отчёт недоступен, нужна подписка)

**Результат:** Не подходит для получения нужной структуры данных.

### Подход 4: Сбор данных из разных эндпоинтов ✅ (ТЕКУЩИЙ)
**Идея:** Собирать данные по кусочкам из разных API и объединять их.

**Реализация:**
1. `/api/v1/supplier/sales` - данные о продажах (заказано, выкупили)
2. `/api/v1/supplier/cards/list` - информация о товарах (бренд, предмет, артикул поставщика)
3. `/api/v1/supplier/stocks` - остатки по складам

**Статус:** Реализовано, но требует тестирования. Методы добавлены в `wb_sales_parser.py`:
- `get_sales_data()` - получение продаж
- `get_product_cards()` - получение карточек товаров
- `get_stocks_data()` - получение остатков
- `build_combined_report()` - объединение данных

**Приоритет:** Метод `download_report_to_excel()` сначала пробует собрать данные из разных эндпоинтов, затем использует старые методы как fallback.

## 🔑 Ключевые инсайты

### 1. API Wildberries очень фрагментирован
- Разные эндпоинты для разных типов данных
- Разные версии API (v1, v2, v5) с разной функциональностью
- Некоторые эндпоинты устарели (404), но не удалены из документации
- Новые API требуют подписки для некоторых функций

### 2. Структура данных неоднородна
- Поля могут называться по-разному (camelCase vs snake_case vs кириллица)
- Некоторые поля могут отсутствовать в зависимости от типа товара
- Остатки могут быть в массиве `warehouses` или напрямую в объекте

### 3. Аутентификация различается по эндпоинтам
- `statistics-api.wildberries.ru` - использует `Authorization: Bearer {token}`
- `seller-analytics-api.wildberries.ru` - использует `HeaderApiKey: {token}` или `Authorization: Bearer {token}`
- `seller-weekly-report.wildberries.ru` - использует `authorizev3: {token}` (JWT токен)

### 4. Формат дат важен
- `reportDetailByPeriod` v1, v2 - требуют `YYYY-MM-DD`
- `reportDetailByPeriod` v5 - требует RFC3339 формат
- `sales` API - использует `YYYY-MM-DD`

### 5. Обработка ошибок критична
- API может вернуть 404 для устаревших эндпоинтов (это нормально)
- API может вернуть 403 для недоступных функций (нужна подписка)
- API может вернуть пустые списки (нет данных за период)
- Нужен fallback на альтернативные методы

## 🛠️ Технические детали

### Используемые библиотеки
- `requests` - HTTP запросы к API
- `pandas` - обработка данных и создание Excel
- `openpyxl` - работа с Excel файлами
- `python-dotenv` - загрузка токенов из .env

### Структура класса WBSalesParser
```python
class WBSalesParser:
    # API эндпоинты
    DETAILED_REPORT_API = "..."  # v5
    DETAILED_REPORT_API_V1 = "..."  # v1
    DETAILED_REPORT_API_V2 = "..."  # v2
    ANALYTICS_API_BASE = "..."  # Новый API
    
    # Методы получения данных
    get_sales_data()           # Продажи
    get_product_cards()         # Карточки товаров
    get_stocks_data()           # Остатки
    build_combined_report()     # Объединение
    
    # Методы работы с отчётами
    download_report_to_excel()  # Основной метод (пробует все подходы)
    get_report_detail()         # Старый метод (fallback)
```

### Логика работы download_report_to_excel()
1. **Попытка 1:** Собрать данные из разных эндпоинтов (новый подход)
   - Получить продажи из `/api/v1/supplier/sales`
   - Получить карточки товаров из `/api/v1/supplier/cards/list`
   - Получить остатки из `/api/v1/supplier/stocks`
   - Объединить данные в нужную структуру

2. **Попытка 2:** Новый Analytics API (seller-analytics-api)
   - Пробует `STOCK_HISTORY_REPORT_CSV`
   - Пробует `DETAIL_HISTORY_REPORT`
   - Проверяет структуру данных

3. **Попытка 3:** Старый API reportDetailByPeriod
   - Пробует v1, v2, v5
   - Проверяет наличие нужных полей

## ⚠️ Известные проблемы

### 1. API карточек товаров может требовать другую аутентификацию
- Метод `get_product_cards()` использует `/api/v1/supplier/cards/list`
- Может потребоваться другой заголовок авторизации
- Может потребоваться другой формат запроса

### 2. Данные о продажах могут не содержать склад и размер
- В `/api/v1/supplier/sales` может не быть `warehouseName` и `techSize`
- Нужно использовать пустые строки или значения по умолчанию
- Может потребоваться дополнительный API для получения этой информации

### 3. Остатки могут быть в разных форматах
- Новый формат: массив `warehouses` внутри объекта
- Старый формат: поля напрямую в объекте
- Код обрабатывает оба варианта

### 4. Некоторые поля недоступны через API
- "Контракт" - не доступно через API, оставляем пустым
- "Сезон" и "Коллекция" - могут отсутствовать в карточках товаров

## 🚀 Рекомендации для дальнейшей работы

### 1. Тестирование нового подхода
- Протестировать `get_sales_data()` на реальных данных
- Протестировать `get_product_cards()` - возможно нужна другая аутентификация
- Протестировать `get_stocks_data()` - проверить формат данных
- Протестировать `build_combined_report()` - проверить корректность объединения

### 2. Альтернативные источники данных
- Если API карточек не работает, можно попробовать:
  - `/api/v1/supplier/info` - информация о товарах
  - `/api/v1/supplier/cards/filter` - фильтрация карточек
  - Другие эндпоинты из документации

### 3. Улучшение обработки ошибок
- Добавить retry логику для временных ошибок
- Добавить более детальное логирование
- Добавить сохранение промежуточных данных для отладки

### 4. Оптимизация производительности
- Кэширование карточек товаров (они не меняются часто)
- Параллельная загрузка данных из разных API
- Оптимизация объединения больших объёмов данных

### 5. Документация API
- Документировать все используемые эндпоинты
- Документировать структуру ответов
- Документировать известные ограничения

## 📝 Важные замечания

### О токенах
- Токены хранятся в `.env` файле
- Файл `.env` в `.gitignore` (не коммитится)
- Токены для разных кабинетов могут иметь разные права доступа
- Некоторые API требуют токены с расширенными правами

### О структуре отчёта
- Желаемая структура определена в `example_parse.xlsx`
- Отчёты должны быть по складам (одна строка = один товар на одном складе)
- Отчёты должны включать все товары, даже без продаж (если есть остатки)

### О датах
- Отчёты скачиваются за вчерашний день по умолчанию
- Можно указать конкретный период через аргументы
- API может не иметь данных за сегодняшний день (данные обновляются с задержкой)

### О множественных кабинетах
- Скрипт обрабатывает все кабинеты из `.env` автоматически
- Для каждого кабинета создаётся отдельный файл отчёта
- Ошибка в одном кабинете не останавливает обработку других

## 🔍 Полезные ссылки

- Документация Wildberries API: https://dev.wildberries.ru/
- Статистика API: https://statistics-api.wildberries.ru/
- Analytics API: https://seller-analytics-api.wildberries.ru/
- Личный кабинет продавца: https://seller.wildberries.ru/

## 💡 Советы для быстрого старта

1. **Начните с чтения `example_parse.xlsx`** - это эталон желаемой структуры
2. **Изучите `wb_sales_parser.py`** - основной класс с логикой работы
3. **Проверьте `.env`** - убедитесь, что все токены на месте
4. **Запустите тестовый запрос** - проверьте, какие данные возвращает API
5. **Используйте логирование** - добавьте print() для отладки
6. **Тестируйте на одном кабинете** - перед массовой обработкой

## 🎓 Уроки, извлечённые из опыта

1. **Не полагайтесь на один API** - всегда нужен fallback
2. **Проверяйте структуру данных** - API может вернуть неожиданный формат
3. **Обрабатывайте все варианты** - поля могут отсутствовать или называться по-разному
4. **Логируйте всё** - это поможет понять, что происходит
5. **Тестируйте на реальных данных** - синтетические данные могут не показать проблемы
6. **Документируйте находки** - это сэкономит время в будущем

---

**Последнее обновление:** Декабрь 2025  
**Текущий статус:** Реализован подход сбора данных из разных эндпоинтов, требует тестирования

