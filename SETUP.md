# Инструкция по настройке проекта на новом устройстве

Полная инструкция по установке и настройке проекта для автоматического скачивания отчётов Wildberries.

## Требования

- Python 3.8 или выше
- Доступ к интернету
- API токены от Wildberries для каждого кабинета

## Шаг 1: Установка Python

### Windows

1. Скачайте Python с официального сайта: https://www.python.org/downloads/
2. При установке **обязательно** отметьте галочку "Add Python to PATH"
3. Проверьте установку:
```bash
python --version
```

### Linux/Mac

Обычно Python уже установлен. Проверьте версию:
```bash
python3 --version
```

Если Python не установлен:
- **Ubuntu/Debian**: `sudo apt update && sudo apt install python3 python3-pip`
- **Mac**: `brew install python3`

## Шаг 2: Клонирование/Копирование проекта

### Вариант А: Клонирование из GitHub

```bash
git clone https://github.com/ваш_username/WBagentforDasha.git
cd WBagentforDasha
```

### Вариант Б: Копирование файлов

Скопируйте все файлы проекта в папку на новом устройстве.

## Шаг 3: Установка зависимостей

Откройте терминал/командную строку в папке проекта и выполните:

```bash
pip install -r requirements.txt
```

Или для Linux/Mac:
```bash
pip3 install -r requirements.txt
```

**Установленные библиотеки:**
- `requests` - для работы с API
- `python-dotenv` - для чтения .env файла
- `pandas` - для работы с данными
- `openpyxl` - для создания Excel файлов

## Шаг 4: Настройка .env файла

1. Скопируйте файл `.env_sample` в `.env`:
   ```bash
   # Windows PowerShell
   Copy-Item .env_sample .env
   
   # Windows CMD
   copy .env_sample .env
   
   # Linux/Mac
   cp .env_sample .env
   ```

2. Откройте файл `.env` в любом текстовом редакторе

3. Добавьте токены для каждого кабинета:
   ```
   COSMO=ваш_токен_здесь
   MMA=ваш_токен_здесь
   MAB=ваш_токен_здесь
   MAU=ваш_токен_здесь
   DREAMLAB=ваш_токен_здесь
   BEAUTYLAB=ваш_токен_здесь
   ```

4. **ВАЖНО:** 
   - Не добавляйте пробелы вокруг знака `=`
   - Не используйте кавычки вокруг токена
   - Каждый токен на отдельной строке

## Шаг 5: Получение API токенов Wildberries

Для каждого кабинета:

1. Войдите в личный кабинет продавца на Wildberries
2. Перейдите в раздел **«Настройки»** → **«Доступ к API»**
3. Нажмите **«Создать токен»**
4. Укажите название токена (например, "Автоматизация отчётов")
5. Выберите категорию доступа: **«Статистика»**
6. Сохраните сгенерированный токен (он больше не будет показан!)
7. Добавьте токен в файл `.env` напротив соответствующего кабинета

## Шаг 6: Проверка работы

Запустите скрипт для проверки:

```bash
python src/wb_report_downloader.py --yesterday
```

Или просто:
```bash
python src/wb_report_downloader.py
```

**Ожидаемый результат:**
- Скрипт найдёт все кабинеты с токенами
- Создаст папку `data/` (если её нет)
- Создаст папку `data/03.12.2025/` с сегодняшней датой
- Скачает отчёты за вчерашний день для каждого кабинета
- Сохранит файлы в папку с датой: `data/03.12.2025/COSMO_03.12.2025.xlsx`, `data/03.12.2025/MMA_03.12.2025.xlsx` и т.д.

## Структура файлов проекта

```
WBagentforDasha/
├── .env                    # Файл с токенами (НЕ коммитить в Git!)
├── .env_sample             # Шаблон .env файла
├── .gitignore             # Игнорируемые файлы
├── requirements.txt       # Зависимости Python
├── src/                   # Папка с исходным кодом
│   ├── __init__.py        # Инициализация пакета
│   ├── wb_sales_parser.py # Основной класс для работы с API
│   └── wb_report_downloader.py # Скрипт для скачивания отчётов
├── data/                  # Папка с отчётами (создаётся автоматически)
│   └── ДД.ММ.ГГГГ/        # Папки по датам (создаются автоматически)
│       ├── COSMO_ДД.ММ.ГГГГ.xlsx
│       ├── MMA_ДД.ММ.ГГГГ.xlsx
│       └── ...
├── README.md              # Основная документация
├── SETUP.md               # Этот файл
└── GIT_COMMANDS.md        # Команды для работы с Git
```

## Использование

### Скачать отчёты за вчера для всех кабинетов

```bash
python src/wb_report_downloader.py --yesterday
```

### Скачать отчёты за конкретный период

```bash
python src/wb_report_downloader.py --period 2024-12-01 2024-12-03
```

### Справка по командам

```bash
python src/wb_report_downloader.py --help
```

## Структура хранения отчётов

Отчёты сохраняются в папке `data/` с организацией по датам:

```
data/
└── 03.12.2025/              # Папка с сегодняшней датой
    ├── COSMO_03.12.2025.xlsx
    ├── MMA_03.12.2025.xlsx
    ├── MAB_03.12.2025.xlsx
    └── ...
```

**Особенности:**
- Папка `data/` создаётся автоматически при первом запуске
- Каждый день создаётся новая папка с датой в формате `ДД.ММ.ГГГГ`
- Файлы отчётов сохраняются в папку с сегодняшней датой
- При повторном запуске в тот же день файлы заменяются (не создаются дубликаты)

## Настройка автоматического запуска

### Windows (Планировщик заданий)

1. Откройте **Планировщик заданий** (Task Scheduler)
2. Создайте новое задание (Create Basic Task)
3. Название: "WB Reports Downloader"
4. Триггер: Ежедневно в нужное время (например, 9:00)
5. Действие: Запустить программу
   - Программа: `python`
   - Аргументы: `C:\путь\к\проекту\src\wb_report_downloader.py --yesterday`
   - Рабочая папка: `C:\путь\к\проекту`
6. Сохраните задание

### Linux/Mac (Cron)

Откройте crontab:
```bash
crontab -e
```

Добавьте строку (запуск каждый день в 9:00):
```bash
0 9 * * * cd /путь/к/проекту && /usr/bin/python3 src/wb_report_downloader.py --yesterday
```

## Решение проблем

### Ошибка: "API токен не найден"

**Решение:**
1. Проверьте, что файл `.env` существует в корне проекта
2. Убедитесь, что токены указаны правильно (без пробелов вокруг `=`)
3. Проверьте, что файл сохранён в кодировке UTF-8

### Ошибка: "ModuleNotFoundError: No module named 'requests'"

**Решение:**
```bash
pip install -r requirements.txt
```

### Ошибка: "HTTP ошибка: 401"

**Решение:**
- Токен неверный или истёк срок действия
- Проверьте токен в личном кабинете Wildberries
- Создайте новый токен при необходимости

### Ошибка: "HTTP ошибка: 404"

**Решение:**
- Это нормально для детализированного API
- Скрипт автоматически переключится на базовый API
- Отчёты будут скачаны успешно

### Файлы не создаются

**Решение:**
1. Проверьте права на запись в папку проекта
2. Убедитесь, что нет открытых Excel файлов с таким же именем
3. Проверьте, что есть данные за указанный период

### Скрипт останавливается на ошибке

**Это нормальное поведение!** Скрипт останавливается при ошибке, чтобы вы могли исправить проблему. После исправления запустите скрипт снова.

## Безопасность

⚠️ **ВАЖНО:**
- Файл `.env` содержит секретные токены
- **НЕ коммитьте** `.env` в Git репозиторий
- Файл `.gitignore` уже настроен для игнорирования `.env`
- Не передавайте `.env` файл другим людям
- Не публикуйте токены в открытом доступе

## Обновление проекта

Если проект обновляется:

1. Получите последние изменения:
   ```bash
   git pull
   ```

2. Обновите зависимости:
   ```bash
   pip install -r requirements.txt --upgrade
   ```

3. Проверьте, не изменился ли формат `.env` файла (сравните с `.env_sample`)

## Дополнительная информация

- Документация Wildberries API: https://dev.wildberries.ru/
- Основной README: см. файл `README.md`
- Команды Git: см. файл `GIT_COMMANDS.md`

## Поддержка

При возникновении проблем:
1. Проверьте раздел "Решение проблем" выше
2. Убедитесь, что все шаги настройки выполнены правильно
3. Проверьте логи выполнения скрипта

---

**Готово!** Проект настроен и готов к работе. 🚀

