# WBagentforDasha - Автоматизация выгрузки отчётов Wildberries

Автоматический скрипт для скачивания отчётов о продажах с платформы Wildberries для нескольких кабинетов.

## 📋 Оглавление

- [Текущий статус проекта](#текущий-статус-проекта)
- [Функционал](#функционал)
- [Требования](#требования)
- [Установка на новом ПК](#установка-на-новом-пк)
- [Настройка](#настройка)
- [Запуск](#запуск)
- [Решение проблем](#решение-проблем)
- [Структура проекта](#структура-проекта)
- [История изменений](#история-изменений)

---

## 🎯 Текущий статус проекта

**Текущая версия:** `d6be6a2` (11.12.2025)

**Статус:** ✅ **ГОТОВ К ИСПОЛЬЗОВАНИЮ**

### Что работает:

- ✅ Автоматический запуск Yandex Browser с профилем
- ✅ Авторизация на Wildberries (с сохранением сессии)
- ✅ Переключение между 6 кабинетами
- ✅ **Удаление всех отчётов в кабинете перед скачиванием**
- ✅ Скачивание отчётов за указанную дату (по умолчанию - вчерашний день)
- ✅ Обработка файлов (переименование, исправление заголовков)
- ✅ Создание резервных копий в папке `data`
- ✅ Правильная обработка объединённых ячеек в Excel
- ✅ **Предупреждение перед запуском** (закрытие процессов, инструкции)
- ✅ **Автоматическая установка на новом устройстве** (одна команда)

### Обрабатываемые кабинеты:

1. beautylab (121614)
2. cosmo (174711)
3. dreamlab (4428365)
4. mab (1140223)
5. mau (53607)
6. mma (224650)

---

## 🚀 Функционал

### Основные возможности:

1. **Автоматическая авторизация** на Wildberries
   - Ввод номера телефона
   - Запрос кодов авторизации (SMS и email)
   - Сохранение сессии в профиле браузера

2. **Работа с несколькими кабинетами**
   - Автоматическое переключение между кабинетами
   - Поиск по ID кабинета
   - Обработка каждого кабинета по очереди

3. **Скачивание отчётов**
   - Отчёты о продажах за указанную дату (по умолчанию - вчерашний день)
   - Возможность указать дату через параметр `--date DD.MM.YYYY`
   python -m src.main --date 10.12.2025
   - **Автоматическое удаление всех существующих отчётов перед скачиванием**
   - Скачивание в формате Excel

4. **Обработка файлов**
   - Переименование: `[название_кабинета] [дата].xlsx`
   - **Исправление заголовков:**
     - Разъединение объединённых ячеек
     - Удаление первой строки
     - Вставка правильных заголовков (A-P столбцы)
   - Создание резервной копии в `data/[дата]/`

5. **Заголовки в обработанных файлах:**
   - A: Бренд
   - B: Предмет
   - C: Сезон
   - D: Коллекция
   - E: Наименование
   - F: Артикул поставщика
   - G: Номенклатура
   - H: Баркод
   - I: Размер
   - J: Контракт
   - K: Склад
   - L: Заказано шт
   - M: Заказано себестоимость
   - N: Выкупили шт
   - O: Выкупили руб
   - P: Текущий остаток

---

## 💻 Требования

### Обязательные:

- **Python:** 3.12+ (проверено на 3.12)
- **Yandex Browser:** версия 140+ (установлен по умолчанию)
- **ОС:** Windows 10/11

### Python пакеты:

Все зависимости в `requirements.txt`:
- `selenium` - автоматизация браузера
- `undetected-chromedriver` - обход анти-бот защиты
- `openpyxl` - работа с Excel файлами
- `pydantic` и `pydantic-settings` - настройки из .env
- `loguru` - логирование
- `python-dotenv` - работа с .env файлами

---

## 📦 Установка на новом ПК

### Быстрая установка (одна команда):

```bash
python setup.py
```

Скрипт автоматически выполнит:
1. ✅ Проверку версии Python
2. ✅ Установку всех зависимостей
3. ✅ Создание файла `.env` из `.env_sample`
4. ✅ Проверку наличия Yandex Browser

### Ручная установка (если нужен контроль):

#### Шаг 1: Клонирование репозитория

```bash
git clone <URL_репозитория>
cd WBagentforDasha
```

#### Шаг 2: Установка Python

1. Скачайте Python 3.12+ с [python.org](https://www.python.org/downloads/)
2. При установке **обязательно отметьте** "Add Python to PATH"
3. Проверьте установку:

```bash
python --version
```

#### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

#### Шаг 4: Установка Yandex Browser

1. Скачайте с [browser.yandex.ru](https://browser.yandex.ru/)
2. Установите по умолчанию: `C:\Users\[ИМЯ]\AppData\Local\Yandex\YandexBrowser\`

---

## ⚙️ Настройка

### Шаг 1: Создание файла `.env`

Скопируйте `.env_sample` в `.env`:

```bash
copy .env_sample .env
```

### Шаг 2: Заполнение `.env`

Откройте `.env` и заполните:

```env
# Номер телефона для авторизации (формат: +79991234567)
PHONE_NUMBER=+79991234567

# Путь к Yandex Browser (обычно по умолчанию)
YANDEX_BROWSER_PATH=%LOCALAPPDATA%\Yandex\YandexBrowser\Application\browser.exe

# User Data (обычно по умолчанию)
YANDEX_USER_DATA_DIR=%LOCALAPPDATA%\Yandex\YandexBrowser\User Data

# Профиль (НЕ МЕНЯТЬ! Используется изолированный профиль)
YANDEX_PROFILE_NAME=WB_Automation
```

⚠️ **ВАЖНО:** 
- Замените `PHONE_NUMBER` на свой номер телефона
- Остальные настройки менять НЕ нужно

### Шаг 3: Первая авторизация

Выполните **первую авторизацию вручную** для сохранения сессии:

```bash
python manual_auth.py
```

1. Откроется Yandex Browser
2. Введите номер телефона
3. Введите коды авторизации (SMS и email)
4. Дождитесь перехода на страницу отчётов
5. Закройте браузер

✅ Теперь авторизация сохранена в профиле `yandex_automation_profile`

---

## 🎬 Запуск

### Основной скрипт:

```bash
python -m src.main
```

**Перед запуском скрипт:**
1. ⚠️ Покажет предупреждение с инструкциями
2. ✅ Автоматически закроет все процессы Yandex Browser
3. ✅ Запросит подтверждение от пользователя

**После подтверждения скрипт выполнит:**
1. Запуск браузера с сохранённой авторизацией
2. Переход на страницу отчётов Wildberries
3. **Удаление всех существующих отчётов в каждом кабинете** (перед скачиванием)
4. Скачивание отчётов для всех 6 кабинетов
5. Обработку и сохранение файлов

### Скачивание отчётов за конкретную дату:

```bash
python -m src.main --date 10.12.2025
```

**Формат даты:** `DD.MM.YYYY` (например: `10.12.2025`)

**По умолчанию:** скачиваются отчёты за вчерашний день

### Ручное исправление заголовков:

Если нужно исправить заголовки в уже скачанных файлах:

```bash
python fix_headers_final.py
```

---

## 🔧 Решение проблем

### Проблема 1: Браузер не запускается

**Симптомы:**
```
SessionNotCreatedException: session not created: cannot connect to chrome
```

**Решения:**

1. **Закройте все процессы Yandex Browser:**

```bash
taskkill /F /IM browser.exe
```

2. **Удалите профиль и авторизуйтесь заново:**

```bash
Remove-Item -Recurse -Force .\yandex_automation_profile
python manual_auth.py
```

3. **Проверьте версию браузера:**
   - Откройте Yandex Browser
   - Перейдите в `browser://version/`
   - Убедитесь, что версия 140+

### Проблема 2: Браузер закрывается при открытии WB

**Симптомы:**
```
InvalidSessionIdException: target window already closed
```

**Решения:**

1. **Проверьте, что профиль не занят:**

```bash
taskkill /F /IM browser.exe
```

2. **Увеличьте задержки в настройках** (`.env`):

```env
DELAY_PAGE_LOAD=6.0
```

3. **Попробуйте запустить через manual_auth.py:**

```bash
python manual_auth.py
```

### Проблема 3: Требуется повторная авторизация

**Симптомы:**
- Скрипт запрашивает номер телефона каждый раз

**Причина:**
- Профиль `yandex_automation_profile` был удалён

**Решение:**

```bash
python manual_auth.py
```

### Проблема 4: Неправильные заголовки в файлах

**Симптомы:**
- В первой строке Excel файла неполные заголовки
- Ячейки объединены

**Решение:**

Запустите скрипт исправления:

```bash
python fix_headers_final.py
```

Или для одного файла:

```python
from pathlib import Path
from openpyxl import load_workbook

file_path = Path("data/11.12.2025/beautylab_11.12.2025.xlsx")
wb = load_workbook(file_path)
ws = wb.active

# Разъединение ячеек
for merged_range in list(ws.merged_cells.ranges):
    if merged_range.min_row == 1:
        ws.unmerge_cells(str(merged_range))

# Удаление первой строки
ws.delete_rows(1)

# Запись заголовков
headers = ["Бренд", "Предмет", "Сезон", ...]  # полный список
for i, h in enumerate(headers, 1):
    ws.cell(1, i).value = h

wb.save(file_path)
```

### Проблема 5: ReadTimeoutError

**Симптомы:**
```
ReadTimeoutError: Read timed out. (read timeout=120)
```

**Решение:**

Уже исправлено в коде (используется `page_load_strategy = 'eager'`).

Если проблема повторяется:
1. Закройте все браузеры
2. Перезапустите скрипт

### Проблема 6: StaleElementReferenceException

**Симптомы:**
```
StaleElementReferenceException: element is not attached to the page
```

**Решение:**

Уже исправлено в коде:
- Добавлены retry механизмы
- Используется JavaScript для очистки полей
- Явные ожидания элементов

---

## 📁 Структура проекта

```
WBagentforDasha/
│
├── src/
│   ├── agents/
│   │   └── browser_agent.py      # Основная логика
│   ├── config/
│   │   └── settings.py            # Настройки из .env
│   ├── utils/
│   │   └── logger.py              # Логирование
│   └── main.py                    # Точка входа
│
├── data/                          # Обработанные файлы (не в git)
│   └── [дата]/
│       └── [кабинет]_[дата].xlsx
│
├── downloads/                     # Скачанные файлы (не в git)
│   └── *.xlsx
│
├── logs/                          # Логи (не в git)
│   ├── app_[дата].log
│   └── errors_[дата].log
│
├── yandex_automation_profile/     # Профиль браузера (не в git)
│   └── Default/
│       └── [данные сессии]
│
├── .env                           # Настройки (НЕ В GIT!)
├── .env_sample                    # Пример настроек
├── requirements.txt               # Зависимости Python
├── manual_auth.py                 # Скрипт для авторизации
├── fix_headers_final.py           # Скрипт для исправления заголовков
├── algorithm                      # Алгоритм работы скрипта
├── CHANGELOG_SESSION.md           # Лог изменений
└── README.md                      # Этот файл
```

---

## ⚠️ Что НЕ пушится в GitHub

### Файлы и папки в `.gitignore`:

1. **`.env`** - Содержит номер телефона и настройки
   - ✅ Передавать: Создайте новый `.env` из `.env_sample`

2. **`yandex_automation_profile/`** - Профиль браузера с сохранённой авторизацией
   - ⚠️ **НЕ передавать!** Каждый ПК должен создать свой профиль через `manual_auth.py`
   - Причина: Профиль привязан к конкретному ПК и пользователю

3. **`data/`** - Обработанные файлы отчётов
   - ✅ Передавать: Если нужны исторические данные
   - Способ: ZIP архив или облако

4. **`downloads/`** - Временные скачанные файлы
   - ❌ НЕ передавать: Временные файлы

5. **`logs/`** - Логи выполнения
   - ❌ НЕ передавать: Специфичны для каждого ПК

### Для переноса на новый ПК нужно:

#### Вариант 1: Чистая установка (рекомендуется)

1. Клонировать репозиторий
2. Установить зависимости
3. Создать `.env` из `.env_sample`
4. Запустить `python manual_auth.py` для первой авторизации

#### Вариант 2: С историческими данными

1. Выполнить шаги из Варианта 1
2. Скопировать папку `data/` на новый ПК
3. Поместить в корень проекта

⚠️ **НЕ копируйте `yandex_automation_profile/` на другой ПК!**
Это приведёт к проблемам с авторизацией.

---

## 📝 История исправлений багов

### Баг 1: JSONDecodeError при запуске браузера

**Проблема:**
```
json.decoder.JSONDecodeError: Expecting ',' delimiter
```

**Причина:**
- `undetected-chromedriver` пытался читать повреждённый файл `Preferences` из профиля

**Решение:**
```python
self.driver = uc.Chrome(
    options=options,
    browser_executable_path=str(browser_path),
    version_main=140,
    use_subprocess=False,  # ← Это исправило проблему
)
```

**Коммит:** `e8c7e14`

---

### Баг 2: Браузер закрывается при открытии WB

**Проблема:**
```
InvalidSessionIdException: target window already closed
```

**Причина:**
- Анти-бот защита Wildberries
- CDP команда `Page.setDownloadBehavior` вызывала краш

**Решение:**
```python
# Обернули CDP команду в try-except
try:
    self.driver.execute_cdp_cmd("Page.setDownloadBehavior", {
        "behavior": "allow",
        "downloadPath": str(self.downloads_dir.absolute())
    })
except Exception as cdp_error:
    logger.warning(f"⚠ Не удалось установить папку скачивания: {cdp_error}")
```

**Коммит:** `e8c7e14`

---

### Баг 3: Неправильные заголовки в Excel файлах

**Проблема:**
- Заголовки записывались, но не сохранялись
- Ячейки оставались пустыми (None)

**Причина:**
- Wildberries выгружает файлы с **объединёнными ячейками** в первой строке:
  - A1:K1 объединены (значение "Бренд")
  - L1:M1 объединены (значение "Заказано шт")
  - N1:O1 объединены (значение "Выкупили шт")

**Решение:**
```python
# Сначала разъединяем ячейки
merged_ranges = list(ws.merged_cells.ranges)
for merged_range in merged_ranges:
    if merged_range.min_row == 1 and merged_range.max_row == 1:
        ws.unmerge_cells(str(merged_range))

# Затем удаляем первую строку
ws.delete_rows(1)

# И записываем заголовки
for col_idx, header in enumerate(headers, start=1):
    ws.cell(row=1, column=col_idx).value = header
```

**Коммит:** `c10e06a`

---

### Баг 4: StaleElementReferenceException при переключении кабинетов

**Проблема:**
```
StaleElementReferenceException: element is not attached to the page
```

**Причина:**
- После перехода между страницами DOM обновляется
- Старые ссылки на элементы становятся невалидными

**Решение:**
```python
# Retry механизм в fill_input
for attempt in range(3):
    try:
        element = WebDriverWait(self.driver, 10).until(
            EC.presence_of_element_located((by, value))
        )
        # Используем JavaScript для очистки
        self.driver.execute_script("arguments[0].value = '';", element)
        element.send_keys(text)
        break
    except StaleElementReferenceException:
        if attempt < 2:
            time.sleep(1)
            continue
        else:
            raise
```

**Коммит:** В рабочей версии `7301b2b`

---

### Баг 5: ReadTimeoutError - браузер не отвечает

**Проблема:**
```
ReadTimeoutError: Read timed out. (read timeout=120)
```

**Причина:**
- Браузер долго загружает страницу
- Ожидает загрузку всех ресурсов (картинки, скрипты)

**Решение:**
```python
# Используем 'eager' стратегию загрузки
options.page_load_strategy = 'eager'  # Не ждать полной загрузки
```

**Статус:** Исправлено, потом убрано. В текущей версии не используется.

---

## 🔄 Откат к предыдущим версиям

Если что-то пошло не так, можно откатиться:

### К текущей стабильной версии:
```bash
git reset --hard d6be6a2  # С исправлением заголовков
```

### К версии без обработки заголовков:
```bash
git reset --hard e8c7e14
```

### К оригинальной рабочей версии:
```bash
git reset --hard 7301b2b
```

### Просмотр истории:
```bash
git log --oneline -10
```

### Просмотр изменений в коммите:
```bash
git show <commit-hash>
```

---

## 📞 Контакты и поддержка

При проблемах:
1. Проверьте раздел [Решение проблем](#решение-проблем)
2. Проверьте логи в `logs/errors_[дата].log`
3. Прочитайте `CHANGELOG_SESSION.md` для детальной истории изменений

---

## 📄 Лицензия

Проект для личного использования.

---

## 🎉 Changelog (краткий)

- **d6be6a2** (11.12.2025) - Обновлена документация
- **c10e06a** (11.12.2025) - ✅ Добавлена обработка объединённых ячеек в заголовках
- **91c93fb** (11.12.2025) - Добавлена документация для отката
- **e8c7e14** (11.12.2025) - Исправлены проблемы с запуском браузера и авторизацией
- **7301b2b** - Оригинальная рабочая версия (скачивание отчётов работает)

Полная история в `CHANGELOG_SESSION.md`.

