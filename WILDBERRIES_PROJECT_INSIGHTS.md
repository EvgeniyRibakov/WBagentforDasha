# Инсайты и советы для проекта парсера Wildberries

Этот документ содержит инсайты, советы и рекомендации для создания проекта парсера цен Wildberries на основе опыта разработки парсера Ozon. Проект будет использовать идентичный human-агент подход.

---

## 🔍 Ключевые отличия Wildberries от Ozon

### 1. Структура личного кабинета

**Wildberries Seller:**
- **Стартовая страница:** `https://seller.wildberries.ru/analytics-reports/sales` (раздел отчётов о продажах)
- Структура более сложная, с множеством разделов
- Часто использует SPA (Single Page Application) с динамической загрузкой контента
- Может требовать более длительных ожиданий загрузки данных
- **Важно:** Стартовая страница уже указывает на раздел отчётов, навигация к нему не требуется

**Рекомендации:**
- Используйте `WebDriverWait` с увеличенными таймаутами (15-20 секунд)
- Проверяйте готовность страницы через JavaScript: `document.readyState === 'complete'`
- Ожидайте появления специфичных элементов, а не просто загрузки страницы

### 2. Авторизация и использование профиля Chrome

**⚠️ ВАЖНО: Авторизация НЕ требуется!**

Проект использует профиль Chrome, в котором **уже выполнена авторизация** в кабинеты Wildberries. Это означает:

- **Авторизация пропускается полностью** - скрипт сразу переходит к работе с отчётами
- **Сессия сохраняется** в профиле Chrome между запусками
- **Cookies и токены** уже присутствуют в профиле
- **Не требуется ввод кодов 2FA** при каждом запуске

**Как это работает:**

1. **Подготовка профиля Chrome (один раз):**
   - Откройте Chrome с нужным профилем вручную
   - Авторизуйтесь на Wildberries Seller один раз
   - Убедитесь, что авторизация сохранена (можно закрыть и открыть снова)
   - Закройте Chrome полностью

2. **Использование в скрипте:**
   - Скрипт запускает Chrome с указанным профилем
   - Chrome автоматически загружает сохранённые cookies и сессию
   - Скрипт сразу переходит на стартовую страницу отчётов
   - Авторизация не требуется, так как сессия уже активна

**Рекомендации:**
- Убедитесь, что Chrome полностью закрыт перед запуском скрипта
- Проверяйте, что профиль содержит активную сессию (периодически обновляйте вручную)
- Если сессия истекла, авторизуйтесь вручную в профиле Chrome снова

### 3. Защита от ботов

**Wildberries может быть более строгим:**
- Частые проверки на человеческое поведение
- Анализ паттернов движения мыши
- Проверка User-Agent и других заголовков браузера
- Возможны блокировки при слишком частых запросах

**Рекомендации:**
- Используйте те же техники, что и в Ozon проекте:
  - Задержки между действиями (1.5-3 секунды)
  - Посимвольный ввод текста
  - Прокрутка страницы перед кликами
  - Случайные микропаузы
- Добавьте случайные движения мыши между действиями
- Используйте реальный User-Agent (обновляйте регулярно)
- Рассмотрите использование undetected-chromedriver или selenium-stealth

### 4. Структура данных и отчёты

**Wildberries:**
- Отчёты могут быть в разных форматах (XLSX, CSV)
- Названия файлов могут отличаться от Ozon
- Структура данных может быть другой (другие колонки, формат)
- Может быть несколько типов отчётов (цены, остатки, продажи и т.д.)

**Рекомендации:**
- Изучите структуру отчётов Wildberries заранее
- Создайте гибкую систему парсинга Excel/CSV файлов
- Подготовьте маппинг колонок для конвертации данных
- Сохраняйте HTML страниц в `pages_code/` для отладки (как в Ozon проекте)

---

## 🛠 Технические рекомендации

### 1. Архитектура проекта

**Используйте ту же структуру, что и в Ozon проекте:**
```
WildberriesPriceParse/
├── src/
│   ├── agents/
│   │   ├── browser_agent.py      # Основной агент (аналог Ozon)
│   │   └── wildberries_api_client.py  # API клиент (если нужен)
│   ├── bot/                      # Telegram бот (переиспользуйте логику)
│   ├── config/
│   │   └── settings.py           # Настройки (адаптируйте под WB)
│   ├── utils/
│   │   └── logger.py             # Логирование (переиспользуйте)
│   └── main.py                   # Точка входа
├── downloads/                    # Скачанные файлы
├── logs/                         # Логи
├── pages_code/                   # HTML страниц для справки
└── .env                          # Настройки
```

### 2. Настройки браузера

**Критически важные настройки для Wildberries:**

```python
# В browser_agent.py при инициализации Chrome
options = webdriver.ChromeOptions()

# НЕ используйте headless - Wildberries может блокировать
# options.add_argument("--headless=new")  # НЕ включать!

# Базовые настройки (как в Ozon)
options.add_argument("--no-sandbox")
options.add_argument("--disable-dev-shm-usage")
options.add_argument("--disable-blink-features=AutomationControlled")

# Убираем признаки автоматизации
options.add_experimental_option("excludeSwitches", ["enable-automation", "enable-logging"])
options.add_experimental_option('useAutomationExtension', False)

# User-Agent (обновляйте регулярно!)
options.add_argument(
    "user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
    "(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
)

# JavaScript для скрытия webdriver флага
driver.execute_cdp_cmd('Page.addScriptToEvaluateOnNewDocument', {
    'source': '''
        Object.defineProperty(navigator, 'webdriver', {
            get: () => undefined
        })
    '''
})
```

### 3. Задержки и имитация человеческого поведения

**Рекомендуемые задержки (адаптируйте под Wildberries):**

```python
# В settings.py или .env
DELAY_BEFORE_CLICK=1.5-2.5      # Wildberries может требовать больше времени
DELAY_AFTER_CLICK=2.0-3.0       # Увеличьте для стабильности
DELAY_BEFORE_TYPE=0.8-1.2
DELAY_AFTER_TYPE=1.2-1.8
DELAY_BETWEEN_KEYS=0.1-0.15     # Случайные микропаузы
DELAY_PAGE_LOAD=3.0-5.0         # Wildberries может загружаться дольше
```

**Дополнительные техники:**
- Случайные движения мыши между действиями
- Прокрутка страницы перед кликами (даже если элемент виден)
- Небольшие паузы после загрузки динамического контента
- Имитация чтения страницы (пауза 2-3 секунды на текстовых блоках)

### 4. Обработка динамического контента

**Wildberries использует много JavaScript:**

```python
# Ожидание загрузки динамического контента
def wait_for_dynamic_content(self, timeout=20):
    """Ожидает полной загрузки динамического контента."""
    WebDriverWait(self.driver, timeout).until(
        lambda d: d.execute_script("return document.readyState") == "complete"
    )
    # Дополнительная проверка на отсутствие загрузчиков
    WebDriverWait(self.driver, timeout).until(
        lambda d: len(d.find_elements(By.CSS_SELECTOR, ".loader, .spinner, [class*='loading']")) == 0
    )
```

---

## 📋 Пошаговый алгоритм (адаптация под Wildberries)

### ⚠️ Важное отличие: Авторизация НЕ требуется!

Так как используется профиль Chrome с сохранённой авторизацией, алгоритм упрощается:

### Этап 1: Запуск браузера с профилем Chrome

**Что происходит:**
1. Скрипт запускает Chrome с указанным профилем (`CHROME_USER_DATA_DIR` и `CHROME_PROFILE_NAME`)
2. Chrome автоматически загружает сохранённые cookies и сессию Wildberries
3. Браузер готов к работе без авторизации

**Рекомендации:**
- Убедитесь, что Chrome полностью закрыт перед запуском скрипта
- Проверьте, что путь к профилю указан правильно в `.env`
- Если сессия истекла, авторизуйтесь вручную в профиле Chrome один раз

### Этап 2: Переход на стартовую страницу отчётов

**Что происходит:**
1. Скрипт переходит на `https://seller.wildberries.ru/analytics-reports/sales`
2. Страница загружается с уже авторизованной сессией
3. Навигация к разделу отчётов не требуется (стартовая страница уже там)

**Рекомендации:**
- Дождитесь полной загрузки страницы (SPA может загружаться динамически)
- Проверьте, что вы действительно на странице отчётов (по наличию ключевых элементов)
- Сохраняйте HTML страницы в `pages_code/` для анализа структуры

### Этап 3: Выбор и скачивание отчёта

**Особенности:**
- Может быть выпадающий список типов отчётов
- Может быть календарь для выбора периода
- Может быть несколько кнопок "Скачать" (для разных форматов)

**Рекомендации:**
- Сохраняйте HTML страницы выбора отчётов
- Реализуйте поиск файла по паттерну (как в Ozon проекте)
- Поддерживайте разные форматы файлов (XLSX, CSV)

---

## ⚠️ Потенциальные проблемы и решения

### Проблема 1: Блокировка аккаунта

**Симптомы:**
- Капча появляется часто
- Аккаунт временно блокируется
- Сообщения об "подозрительной активности"

**Решения:**
- Увеличьте задержки между действиями
- Используйте профиль Chrome с историей браузера
- Не запускайте парсер слишком часто (минимум раз в час)
- Рассмотрите ротацию User-Agent и других заголовков

### Проблема 2: Элементы не находятся

**Симптомы:**
- `ElementNotFound` ошибки
- Элементы загружаются с задержкой
- Динамический контент не успевает загрузиться

**Решения:**
- Увеличьте таймауты ожидания (до 20-30 секунд)
- Используйте явные ожидания (`WebDriverWait`) вместо неявных
- Проверяйте наличие элементов через несколько селекторов
- Сохраняйте скриншоты при ошибках для отладки

### Проблема 3: Файл не скачивается

**Симптомы:**
- Клик по кнопке "Скачать" не приводит к скачиванию
- Файл скачивается в другую папку
- Файл скачивается с другим именем

**Решения:**
- Проверяйте настройки загрузок в профиле Chrome (как в Ozon проекте)
- Ищите файл в нескольких папках (стандартная Downloads, профиль Chrome)
- Используйте паттерн поиска файла по дате и названию (как в Ozon)
- Проверяйте расширение файла (.xlsx, .csv, .xls)

### Проблема 4: Сессия истекла или авторизация отсутствует

**Симптомы:**
- При переходе на страницу появляется форма авторизации
- Сообщения об истёкшей сессии
- Редирект на страницу входа

**Решения:**
- **Проверьте профиль Chrome:** откройте Chrome с этим профилем вручную и проверьте авторизацию
- **Обновите сессию:** авторизуйтесь вручную в профиле Chrome один раз, затем закройте Chrome полностью
- **Проверьте срок действия cookies:** Wildberries может иметь короткий срок действия сессии (несколько дней/недель)
- **Убедитесь, что Chrome закрыт:** не запускайте Chrome вручную одновременно со скриптом
- **Проверьте путь к профилю:** убедитесь, что `CHROME_USER_DATA_DIR` и `CHROME_PROFILE_NAME` указаны правильно

**Как проверить сессию вручную:**
1. Откройте Chrome с нужным профилем
2. Перейдите на `https://seller.wildberries.ru/analytics-reports/sales`
3. Если требуется авторизация - авторизуйтесь
4. Закройте Chrome полностью
5. Запустите скрипт - сессия должна быть сохранена

---

## 🔧 Специфичные настройки для Wildberries

### Настройки в .env

```env
# Wildberries Seller настройки
WILDBERRIES_START_URL=https://seller.wildberries.ru/analytics-reports/sales
# PHONE_NUMBER и EMAIL НЕ требуются - авторизация через профиль Chrome
# PHONE_NUMBER=+79991234567  # Не используется
# EMAIL=your_email@example.com  # Не используется

# Задержки (адаптированы под Wildberries)
DELAY_BEFORE_CLICK=2.0
DELAY_AFTER_CLICK=2.5
DELAY_BEFORE_TYPE=1.0
DELAY_AFTER_TYPE=1.5
DELAY_BETWEEN_KEYS=0.12
DELAY_PAGE_LOAD=4.0

# Профиль Chrome (ОБЯЗАТЕЛЬНО - содержит сохранённую авторизацию)
# ⚠️ ВАЖНО: В этом профиле должна быть выполнена авторизация в Wildberries Seller
# Как настроить:
# 1. Откройте Chrome с этим профилем вручную
# 2. Авторизуйтесь на https://seller.wildberries.ru/
# 3. Закройте Chrome полностью
# 4. Теперь скрипт будет использовать сохранённую сессию
CHROME_USER_DATA_DIR=%LOCALAPPDATA%\Google\Chrome\User Data
CHROME_PROFILE_NAME=Profile 1

# Telegram бот (переиспользуйте из Ozon проекта)
TELEGRAM_BOT_TOKEN=your_bot_token
TELEGRAM_BOT_PASSWORD=your_password

# Google Sheets (если используется)
UPLOAD_TO_GOOGLE_SHEETS=true
GOOGLE_SHEETS_URL=https://docs.google.com/spreadsheets/d/YOUR_ID/edit
GOOGLE_SHEETS_CREDENTIALS_PATH=google_sheets_credentials.json
```

### Настройки в settings.py

```python
class Settings(BaseSettings):
    # Wildberries Seller
    wildberries_start_url: str = "https://seller.wildberries.ru/analytics-reports/sales"
    # phone_number и email НЕ требуются, так как авторизация через профиль Chrome
    phone_number: str = ""  # Не используется (авторизация через профиль)
    email: str = ""  # Не используется (авторизация через профиль)
    
    # Задержки (увеличены для Wildberries)
    delay_before_click: float = 2.0
    delay_after_click: float = 2.5
    delay_before_type: float = 1.0
    delay_after_type: float = 1.5
    delay_between_keys: float = 0.12
    delay_page_load: float = 4.0
    
    # Остальные настройки аналогичны Ozon проекту
    # ...
```

---

## 📝 Чеклист разработки

### Фаза 1: Исследование и подготовка

- [ ] Изучить структуру личного кабинета Wildberries Seller
- [ ] Сохранить HTML страницы раздела отчётов в `pages_code/`
- [ ] Определить селекторы для ключевых элементов на странице отчётов
- [ ] Изучить процесс скачивания отчётов вручную
- [ ] Проанализировать структуру скачанных файлов
- [ ] **Настроить профиль Chrome:** авторизоваться в Wildberries Seller вручную в нужном профиле

### Фаза 2: Базовая реализация

- [ ] Создать структуру проекта (аналогично Ozon)
- [ ] Реализовать `BrowserAgent` с базовыми методами
- [ ] **Пропустить авторизацию** - использовать профиль Chrome с сохранённой сессией
- [ ] Реализовать переход на стартовую страницу отчётов
- [ ] Реализовать скачивание отчёта

### Фаза 3: Улучшения и стабилизация

- [ ] Добавить обработку ошибок и retry логику
- [ ] Реализовать поиск скачанного файла (как в Ozon)
- [ ] Добавить поддержку профиля Chrome для сохранения сессии
- [ ] Оптимизировать задержки под Wildberries
- [ ] Добавить подробное логирование

### Фаза 4: Интеграции

- [ ] Интегрировать Telegram бот (переиспользовать из Ozon)
- [ ] Добавить загрузку в Google Sheets (если нужно)
- [ ] Реализовать обработку нескольких кабинетов (если применимо)
- [ ] Добавить уведомления об ошибках

---

## 🎯 Рекомендации по переиспользованию кода

### Что можно переиспользовать из Ozon проекта:

1. **Структура проекта** - полностью
2. **Telegram бот** (`src/bot/`) - с минимальными изменениями
3. **Логирование** (`src/utils/logger.py`) - полностью
4. **Настройки** (`src/config/settings.py`) - адаптировать под WB
5. **Методы BrowserAgent:**
   - `start_browser()` - с адаптацией настроек
   - `wait_for_element()` - полностью
   - `click_button()` - полностью
   - `fill_input()` - полностью
   - `get_user_input()` - полностью
   - `_find_downloaded_file()` - адаптировать под паттерны WB
   - `_update_chrome_preferences()` - полностью
   - `open_file()` - полностью

### Что нужно переписать:

1. **Метод `execute_flow()`** - полностью под логику Wildberries (БЕЗ авторизации!)
2. **Селекторы элементов** - все селекторы специфичны для WB
3. **Обработка авторизации** - **НЕ ТРЕБУЕТСЯ** (используется профиль Chrome)
4. **Навигация к отчётам** - **НЕ ТРЕБУЕТСЯ** (стартовая страница уже на отчётах)
5. **Паттерны поиска файлов** - названия файлов могут отличаться от Ozon

---

## 🔐 Безопасность и лучшие практики

### 1. Хранение учётных данных

- **НЕ** храните пароли и токены в коде
- Используйте `.env` файл (уже в `.gitignore`)
- Рассмотрите использование переменных окружения системы
- Для продакшена: используйте секретные менеджеры (AWS Secrets Manager, HashiCorp Vault)

### 2. Обработка ошибок

```python
# Всегда логируйте ошибки с контекстом
try:
    # Действие
    pass
except Exception as e:
    logger.error(f"Ошибка при выполнении действия: {e}")
    logger.exception("Детали ошибки:")  # Полный traceback
    # Делайте скриншот для отладки
    self.driver.save_screenshot("error_screenshot.png")
    raise
```

### 3. Валидация данных

- Проверяйте наличие обязательных настроек перед запуском
- Валидируйте формат данных (номера телефонов, email)
- Проверяйте существование файлов перед обработкой

---

## 📊 Мониторинг и логирование

### Рекомендуемая структура логов:

```
logs/
├── app_2025-01-15.log          # Основной лог
├── errors_2025-01-15.log        # Только ошибки (опционально)
└── debug_2025-01-15.log         # Отладочная информация (опционально)
```

### Уровни логирования:

- `INFO` - основные действия (клики, переходы, скачивания)
- `SUCCESS` - успешное завершение этапов
- `WARNING` - предупреждения (элемент не найден, но продолжается работа)
- `ERROR` - ошибки, требующие внимания
- `DEBUG` - детальная отладочная информация

---

## 🚀 Оптимизация производительности

### 1. Параллельная обработка (если несколько кабинетов)

```python
# Если нужно обработать несколько кабинетов
from concurrent.futures import ThreadPoolExecutor

def process_cabinets(cabinets: list[str]):
    with ThreadPoolExecutor(max_workers=2) as executor:
        futures = [executor.submit(process_cabinet, cab) for cab in cabinets]
        results = [f.result() for f in futures]
    return results
```

**⚠️ Внимание:** Wildberries может блокировать параллельные сессии с одного IP!

### 2. Кэширование

- Кэшируйте список кабинетов между запусками
- Сохраняйте последние селекторы элементов
- Кэшируйте структуру отчётов

### 3. Оптимизация ожиданий

- Используйте явные ожидания вместо `time.sleep()` где возможно
- Уменьшайте таймауты для быстрых операций
- Увеличивайте таймауты только для медленных операций

---

## 📚 Полезные ресурсы

### Документация:

- Selenium Python: https://selenium-python.readthedocs.io/
- WebDriver Wait: https://selenium-python.readthedocs.io/waits.html
- Chrome DevTools Protocol: https://chromedevtools.github.io/devtools-protocol/

### Инструменты для отладки:

- Chrome DevTools - для анализа структуры страниц
- Selenium IDE - для записи действий (как референс)
- Postman/Insomnia - для тестирования API (если доступно)

### Библиотеки:

- `selenium` - браузерная автоматизация
- `undetected-chromedriver` - обход детекции автоматизации (опционально)
- `selenium-stealth` - скрытие признаков автоматизации (опционально)
- `loguru` - логирование (уже используется в Ozon проекте)
- `pydantic` - валидация настроек (уже используется)

---

## ⚡ Быстрый старт (чеклист)

1. **Клонируйте/скопируйте структуру Ozon проекта**
2. **Переименуйте проект** в `WildberriesPriceParse`
3. **Адаптируйте настройки:**
   - Измените URL на Wildberries
   - Обновите названия переменных в `.env`
   - Адаптируйте `settings.py`
4. **Изучите структуру Wildberries Seller:**
   - Сохраните HTML страниц в `pages_code/`
   - Определите селекторы элементов
5. **Реализуйте базовый flow:**
   - Запуск браузера с профилем Chrome (авторизация уже сохранена)
   - Переход на стартовую страницу отчётов
   - Скачивание отчёта
6. **Тестируйте и оптимизируйте:**
   - Проверяйте стабильность
   - Оптимизируйте задержки
   - Добавляйте обработку ошибок
7. **Интегрируйте Telegram бот** (переиспользуйте из Ozon)
8. **Добавьте мониторинг и уведомления**

---

## 🎓 Уроки из Ozon проекта

### Что сработало хорошо:

1. ✅ Использование профиля Chrome для сохранения сессии
2. ✅ Подробное логирование всех действий
3. ✅ Обработка множественных кабинетов
4. ✅ Поиск файлов в нескольких папках
5. ✅ Telegram бот для управления
6. ✅ Имитация человеческого поведения с задержками

### Что можно улучшить:

1. 🔄 Более гибкая система селекторов (fallback варианты)
2. 🔄 Автоматическое обновление User-Agent
3. 🔄 Retry логика для неустойчивых операций
4. 🔄 Метрики производительности (время выполнения этапов)
5. 🔄 Автоматические тесты для критических путей

---

## 🔑 Работа с профилем Chrome (критически важно!)

### Как работает сохранение сессии

**Профиль Chrome содержит:**
- Cookies с токенами авторизации Wildberries
- Local Storage с данными сессии
- IndexedDB с кэшированными данными
- История браузера (помогает выглядеть более "человечно")

**Процесс работы:**

1. **Первый запуск (настройка профиля):**
   ```bash
   # 1. Откройте Chrome с нужным профилем вручную
   # Windows: chrome.exe --profile-directory="Profile 1"
   # Или просто откройте Chrome и выберите нужный профиль
   
   # 2. Авторизуйтесь на Wildberries Seller
   # Перейдите на https://seller.wildberries.ru/
   # Введите логин/пароль, пройдите 2FA (если требуется)
   # Убедитесь, что вы авторизованы
   
   # 3. Закройте Chrome полностью (все окна и процессы)
   ```

2. **Использование в скрипте:**
   ```python
   # Скрипт автоматически:
   # 1. Запускает Chrome с указанным профилем
   # 2. Chrome загружает сохранённые cookies и сессию
   # 3. Переходит на стартовую страницу
   # 4. Сессия уже активна - авторизация не требуется!
   ```

### Важные моменты

**⚠️ Chrome должен быть закрыт:**
- Не запускайте Chrome вручную одновременно со скриптом
- Chrome блокирует доступ к профилю, если он уже открыт
- Скрипт не сможет использовать профиль, если Chrome запущен

**⚠️ Проверка сессии:**
- Периодически проверяйте авторизацию в профиле Chrome вручную
- Если сессия истекла - авторизуйтесь снова один раз
- Wildberries может иметь срок действия сессии (несколько дней/недель)

**⚠️ Путь к профилю:**
- Используйте абсолютный путь или переменные окружения (`%LOCALAPPDATA%`)
- Убедитесь, что путь указан правильно в `.env`
- Имя профиля должно точно совпадать (например, `Profile 1`, а не `Profile1`)

### Определение имени профиля

**Способ 1 (рекомендуется):**
1. Откройте Chrome с нужным профилем
2. Введите в адресной строке: `chrome://version/`
3. Найдите строку "Путь к профилю" или "Profile Path"
4. Имя профиля - это последняя часть пути (например, `Profile 1`)

**Способ 2:**
1. Откройте папку `User Data` в проводнике
2. Там будут папки: `Default`, `Profile 1`, `Profile 2` и т.д.
3. Чтобы понять, какая папка соответствует вашему профилю:
   - Откройте файл `User Data\Local State` в текстовом редакторе
   - Найдите имя вашего профиля (например, "Рабочий профиль")
   - Рядом будет указано имя папки (например, `Profile 1`)

### Пример кода для работы с профилем Chrome

**В методе `start_browser()` (аналогично Ozon проекту):**

```python
def start_browser(self) -> None:
    """Запуск браузера с профилем Chrome."""
    options = webdriver.ChromeOptions()
    
    # НЕ используйте headless - Wildberries может блокировать
    # options.add_argument("--headless=new")  # НЕ включать!
    
    # Базовые настройки
    options.add_argument("--no-sandbox")
    options.add_argument("--disable-dev-shm-usage")
    options.add_argument("--disable-blink-features=AutomationControlled")
    
    # ⚠️ КРИТИЧЕСКИ ВАЖНО: Использование профиля Chrome
    if self.settings.chrome_user_data_dir:
        user_data_path = Path(self.settings.chrome_user_data_dir)
        path_str = str(user_data_path.absolute())
        
        if os.path.exists(path_str):
            # Указываем путь к папке User Data
            options.add_argument(f'--user-data-dir={path_str}')
            # Указываем имя профиля (например, "Profile 1" или "Default")
            options.add_argument(f"--profile-directory={self.settings.chrome_profile_name}")
            
            logger.info(f"Используется профиль Chrome: {path_str} / {self.settings.chrome_profile_name}")
            
            # Проверяем, что папка профиля существует
            profile_path = user_data_path / self.settings.chrome_profile_name
            if os.path.exists(str(profile_path.absolute())):
                logger.success(f"✓ Папка профиля найдена: {profile_path}")
            else:
                logger.warning(f"⚠ Папка профиля не найдена: {profile_path}")
        else:
            logger.warning(f"⚠ Путь к профилю Chrome не существует: {path_str}")
    
    # Убираем признаки автоматизации
    options.add_experimental_option("excludeSwitches", ["enable-automation", "enable-logging"])
    options.add_experimental_option('useAutomationExtension', False)
    
    # Настройка скачивания файлов
    downloads_path = str(self.downloads_dir.absolute())
    prefs = {
        "download.default_directory": downloads_path,
        "download.prompt_for_download": False,
        "download.directory_upgrade": True,
        "safebrowsing.enabled": True,
        "profile.default_content_setting_values.automatic_downloads": 1,
    }
    options.add_experimental_option("prefs", prefs)
    
    # User-Agent
    options.add_argument(
        "user-agent=Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 "
        "(KHTML, like Gecko) Chrome/120.0.0.0 Safari/537.36"
    )
    
    # Запускаем браузер
    service = Service(ChromeDriverManager().install())
    self.driver = webdriver.Chrome(service=service, options=options)
    
    # Убираем признаки автоматизации через JavaScript
    self.driver.execute_cdp_cmd('Page.addScriptToEvaluateOnNewDocument', {
        'source': '''
            Object.defineProperty(navigator, 'webdriver', {
                get: () => undefined
            })
        '''
    })
    
    logger.success("✓ Браузер запущен с профилем Chrome")
```

**В методе `execute_flow()` (упрощённая версия БЕЗ авторизации):**

```python
def execute_flow(self, start_url: str) -> Optional[Path]:
    """Выполнение основного потока работы.
    
    ⚠️ ВАЖНО: Авторизация НЕ требуется - используется профиль Chrome с сохранённой сессией!
    """
    try:
        # Шаг 1: Запуск браузера с профилем Chrome
        # Сессия Wildberries уже сохранена в профиле - авторизация не требуется!
        self.start_browser()
        
        # Шаг 2: Переход на стартовую страницу отчётов
        # URL уже указывает на раздел отчётов: analytics-reports/sales
        self.navigate_to_url(start_url)
        
        # Шаг 3: Проверка, что мы на правильной странице
        # (можно проверить наличие ключевых элементов отчётов)
        logger.info("Проверка загрузки страницы отчётов...")
        time.sleep(self.settings.delay_page_load)
        
        # Шаг 4: Выбор и скачивание отчёта
        # (логика зависит от структуры страницы Wildberries)
        downloaded_file = self._download_report()
        
        return downloaded_file
        
    except Exception as e:
        logger.error(f"Ошибка в процессе выполнения: {e}")
        logger.exception("Детали ошибки:")
        raise
```

**Что происходит при запуске:**

1. Chrome запускается с указанным профилем
2. Chrome автоматически загружает cookies из профиля
3. Cookies содержат токены авторизации Wildberries
4. При переходе на `https://seller.wildberries.ru/analytics-reports/sales` сессия уже активна
5. Авторизация не требуется - можно сразу работать с отчётами!

---

## 📝 Заключение

Проект парсера Wildberries может быть построен на основе архитектуры Ozon проекта с адаптацией под специфику Wildberries. Основные отличия:

- **Авторизация НЕ требуется** → используется профиль Chrome с сохранённой сессией
- **Стартовая страница уже на отчётах** → навигация к разделу не требуется
- **Более строгая защита от ботов** → увеличенные задержки, дополнительные техники обхода
- **Другая структура интерфейса** → новые селекторы для элементов отчётов
- **Возможны другие форматы отчётов** → адаптация парсинга файлов

**Главное:** 
- Используйте профиль Chrome с сохранённой авторизацией - это упрощает проект и делает его более стабильным
- Сохраняйте тот же подход с human-агентом, подробным логированием и обработкой ошибок
- Убедитесь, что Chrome закрыт перед запуском скрипта

---

**Последнее обновление:** 2025-01-15  
**Версия документа:** 1.0.0
